Current Focus, make the slideshow slide right:
* first make a function called slideRight()
- grab the imagesArray
- slice everything left of the images imagesArray
- slice everything right of the images imagesArray
- pop the first item in the left side of the imagesArray
- push it to the right side of the images imagesArray
- grab the middle of the images imagesArray
- push it to the left side

psuedo script:
//existing addImagesToSlideshow for demonstration purposes
function addImagesToSlideshow() {
    const middleElement = findMiddle(0, imagesArray);
    const middleNumber = imagesArray.indexOf(middleElement);
    for (let i = 0; i < imagesArray.length; i++) {
      const newImage = new Image(150, 250);
      newImage.src = imagesArray[i];
      slideShowDiv.append(newImage);
      if (i !== middleNumber) {
        newImage.style.visibility = 'hidden';
      }
    }
  }
  addImagesToSlideshow();
  function slideRight(array) {
     const middleElement = findMiddle(0, imagesArray);
    const index = imagesArray.indexOf(middleElement);
    let leftHalf = array.slice(1, index);
    const firstItem = array.slice(0, 1);
    let rightHalf = array.slice(index + 2, array.length);
    rightHalf = rightHalf.concat(firstItem);
    const newMiddle = array.slice(index + 1, index + 2);
    const element = array.slice(index, index + 1);
    leftHalf = leftHalf.concat(element);
    const newArray = leftHalf.concat(newMiddle, rightHalf);
  }