Current Focus, createDropdown needs to create a dropdown and place it in the dom:
* the first paramater is for the parent of the dropdown
* the second will be label Text
* one of the paramaters has to be an array of options
* it will first create a label
* it will then create a button that will imitate a dropdown button
* it should set the name attribute to the labelText
* it will then create a div that holds all the options
function createDropdown(parent, labelText, [options]) {
 let x = document.createElement("LABEL");
 let t = document.createTextNode(labelText);
 x.setAttribute("for", labelText);
 x.appendChild(t);
 parent.appendChild(x); 
  console.log('creating dropdown');
  const dropDownButton = document.createElement('button');
    dropDownButton.setAttribute("name", labelText);
 const optionsDiv = document.createElement("div");
    for(let i = 0; i < options.length; i++)
    {
        let newSelection = document.createElement("button");
        newSelection.textContent = options[i];
    }

}
export default createDropdown;
